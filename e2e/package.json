{
  "name": "hcc-e2e",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Playwright end-to-end tests for the video-call reconnect fix",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report test-results/html-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
